#Properties to connect to cloud config server
#this file will be loaded before application.properties file when we run spring boot application

spring.cloud.config.uri=http://localhost:8012
spring.cloud.config.name=users

#SEE ENV VARIABLES
spring.cloud.config.username=${CONFIG_SERVER_USERNAME}
spring.cloud.config.password=${CONFIG_SERVER_PASSWORD}